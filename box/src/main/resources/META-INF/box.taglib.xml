<?xml version="1.0" encoding="UTF-8"?>
<facelet-taglib xmlns="https://jakarta.ee/xml/ns/jakartaee"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-facelettaglibrary_4_0.xsd"
                 version="4.0">

    <namespace>http://iffar.edu.br/box</namespace>

    <!-- panel é registrado via @FacesComponent(createTag=true); só o
         behavior "confirm" precisa de taglib manual, já que @FacesBehavior
         não tem esse atalho. handler-class explícito (ConfirmHandler) em vez
         do BehaviorHandler padrão: o padrão avalia "mensagem" uma única vez
         no build da view, o que quebra dentro de h:dataTable/ui:repeat. -->
    <tag>
        <tag-name>confirm</tag-name>
        <handler-class>br.edu.iffar.box.component.confirm.ConfirmHandler</handler-class>
        <attribute>
            <name>mensagem</name>
            <required>false</required>
        </attribute>
    </tag>

</facelet-taglib>
