# Copie este arquivo para .env (nao versionado) e ajuste os valores.
# Ver bpm-app/.env para o comentario completo sobre onde essas variaveis
# sao usadas.

APP_DB_USER=postgres
APP_DB_PASSWORD=postgres
